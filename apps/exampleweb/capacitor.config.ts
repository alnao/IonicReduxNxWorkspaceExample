import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exampleweb',
  webDir: '../../dist/apps/exampleweb',
  bundledWebRuntime: false,
};

export default config;
